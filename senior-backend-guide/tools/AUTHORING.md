# Authoring

1. Edit or add `tools/banks/dNN-slug.json`.
2. Keep `id` / `question` stable when updating answers.
3. Run `node tools/gen.js` (renders banks → fragments → pages).
4. Run `node tools/verify.js`.
